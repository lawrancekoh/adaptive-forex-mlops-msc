---
trigger: always_on
---

# Workspace Rules

## Python Virtual Environment
- BEFORE running any python commands, ALWAYS activate the virtual environment located at `.venv`.
- ENSURE all dependencies are installed in `.venv`.
- If `.venv` is not present, ASK the user before creating one.
